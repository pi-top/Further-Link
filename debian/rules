#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	dpkg-parsechangelog -SVersion > src/lib/further_link/version.py
	dh_auto_build

override_dh_installsystemd:
	dh_installsystemd --name=pt-further-link
