#!/usr/bin/make -f

include /usr/share/dpkg/pkg-info.mk

%:
	dh $@

override_dh_auto_build:
	echo "__version__ = '$(DEB_VERSION)'" > src/lib/further_link/version.py
	[[ -f extra/fl.dat ]] || echo "$(DATA)" > extra/fl.dat
	[[ -f extra/key.aes.pem ]] || echo -e "$(TLS_KEY)" > extra/key.aes.pem

	dh_auto_build

override_dh_clean:
	rm -rf extra/fl.dat extra/key.aes.pem

	dh_clean

override_dh_installsystemd:
	dh_installsystemd --name=pt-further-link
