#!/usr/bin/make -f
%:
	dh $@ --with python3 --with systemd


override_dh_systemd_enable:
    dh_systemd_enable --no-enable --name=pt-further-link

override_dh_systemd_start:
    dh_systemd_start --no-start pt-further-link
